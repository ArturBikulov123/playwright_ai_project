# npm Configuration for Security
# SECURITY: Automatically audit dependencies on install
audit=true
audit-level=moderate

# Use exact versions in package-lock.json for better security
package-lock=true

# Don't allow npm to use potentially unsafe operations
fund=false
package-lock-only=false

