#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Running browser tests in Chrome before push (excluding API tests)..."
npm run test:browser -- --project=chromium

echo "✅ Pre-push checks passed!"
